You can use "docker-compose up" for starting app.

Superuser was created:
	- login "admin"
	- password "admin"

You can use "python manage.py add_user <login> <password>" custom command
inside container to create new user.